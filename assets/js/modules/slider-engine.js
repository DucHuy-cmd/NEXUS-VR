/* NEXUS VR — modules/slider-engine.js   [PHỤ TRÁCH: Trường Vũ]
   TẦNG 2 - REUSABLE MODULES
   Bộ trượt slide DÙNG CHUNG cho Testimonial (trang chủ) và
   Banner nếu có. Viết dạng hàm nhận tham số để tái sử dụng,
   không hardcode riêng cho từng nơi dùng.

   TODO (Trường Vũ): function initSlider(containerSelector, { interval }) { ... }
*/
