/* NEXUS VR — modules/validator.js   [PHỤ TRÁCH: Trường Vũ]
   TẦNG 2 - REUSABLE MODULES
   Module kiểm tra Regex form — dùng chung cho Form Liên hệ
   (contact-controller.js) VÀ Checkout (checkout-controller.js
   của Tường) — nếu Tường cần dùng, gọi hàm ở đây, không viết
   validate riêng.

   TODO (Trường Vũ): viết các hàm ví dụ:
   function isValidEmail(email) { ... }
   function isValidPhoneVN(phone) { ... }   // 0xxxxxxxxx, 10 số
   function isValidName(name) { ... }
*/
